#include "ia.h"

ia::ia()
{


}
int ia::max(int joueur,piece terrain[8][8],int groupe[8][8],int profondeur, int alpha, int beta)
{

    return max;
}
int ia::alphaBeta(int profondeur,int alpha, int beta)
{ /*
    si p est une feuille
{     valeurDeLaPosition = eval(P)
                sortir
    }
    sinon
{     Pour tous les fils F de P et tant que alpha < beta faire
    {       valeurDuFilsCourant = - Alpha_Beta(F, profondeur+1,-beta,-alpha)
                alpha = max(alpha, valeurDuFilsCourant)
    }
 }
*/}
                                                                                                                                     valeurDeLaPosition = alpha     sortir   } }
